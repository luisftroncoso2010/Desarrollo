import styled from "styled-components"
import { MiCaja } from "./style"

/* Heradamos de un componente de estilo ya creado.
NOTA: Las propiedades que excribamos acá son por que se estan sobreescribiendo*/
export const MiBotonLargo = styled(MiCaja)`
    background-color: red;
`